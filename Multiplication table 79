#include <stdio.h>
int main() 
{
int n;
printf("Enter a number: ");
scanf("%d", &n);
if (n <= 1 || n >= 9) 
{
printf("Invalid Input\n");
return 0;
}
for (int i = 1; i <= n; i++) {
printf("%d x %d = %d ", n, i, n * i);
}
return 0;
}
